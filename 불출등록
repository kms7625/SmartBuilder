폼을 열면 다음 문단을 실행한다.
{
        사용자은 전역정보의 "사원명"이다
        사원명은 전역정보의 "사원명"이다
        사원코드는 전역정보의 "사원코드"이다
        전역정보의 "지정폼"은 5이다
 //       거래처그룹의 화면감춤을 설정한다
        창고그룹의 화면감춤을 설정한다
//        사용자는 전역정보의 "사원코드"이다
       

        상태1은 전역정보의 "상태1"이다.
        상태2는 전역정보의 "상태2"이다.
        바코드입력란으로 초점을 이동한다

}

홈버튼을 누르면 다음 문단을 실행한다
{
        "화면을 종료하시겠습니까?"를 화면에 표시한다('예아니오','질문',"홈화면","홈화면예처리","홈화면아니오처리")
}
문단 홈화면예처리
{
        폼을 종료한다
}
문단 홈화면아니오처리
{
        문단의 실행을 취소한다
}
바코드입력란에서 처리항목을 이동하면 다음 문단을 실행한다(탭번호, 탭이동유형)
{
       DB처리문("임시불출저장")을 실행한다.
        임시불출불러오기를 실행한다.
       바코드입력란은 ""을 참조한다
       바코드입력란으로 초점을 이동한다.
       바코드체크1은 조회결과를 참조한다.
       //바코드체크1은 조회결과의 값을 대입한다.
}
문단 임시불출불러오기
{
        //DB처리문("임시불출불러오기")을 실행한다.
        //임시불출불러오기 sql문 직접입력
        DB는 데이터베이스("SHTECH_DB_PDA")를 참조한다
        질의문1은 식(" select IPGO_NUMB, B.ATSK_CODE, A.ITEM_QNTY ")를 참조한다
        질의문2는 식(" from ST_MOVE_INFO_TEMP as A ")를 참조한다
        질의문3은 식(" left join BE_ITEM_INFO as B on A.ITEM_CODE = B.ITEM_CODE ")를 참조한다
        질의문4는 식(" where A.IN_USER = '"+사원코드+"' and A.MEMO_XXXX = 'PDA등록' and MOVE_DATE = '"+불출일자+"' and MOVE_TYPE = '21' ")를 참조한다
        질의문은 식(질의문1 + 질의문2 + 질의문3 + 질의문4)를 참조한다
        명세서배열값은 ""이다
        DB의 질의실행(질의문, 명세서배열값)을 실행한다
        임시불출조회창은 명세서배열값을 참조한다
        //임시불출조회창은 조회결과를 참조한다.
 }
창고입력란을 누르면 다음 문단을 실행한다
{
        창고그룹의 화면감춤을 해제한다
        DB는 데이터베이스("SHTECH_DB_PDA")를 참조한다
        질의문은 식(" select NAME as STOR_NAME, CODE as STOR_CODE  from SYS_CODE_INFO where GRUP_CODE = 'KITT_GUBN'")을 참조한다
        명세서배열값은 ""이다
        DB의 질의실행(질의문, 명세서배열값)을 실행한다
        창고검색창은 명세서배열값을 참조한다
}
창고검색창을 누르면 다음 문단을 실행한다
{
        선택행은 창고검색창의 선택행을 참조한다
        창고입력란은 창고검색창(선택행, "STOR_NAME")을 참조한다
        창고코드는 창고검색창(선택행, "STOR_CODE")을 참조한다
        창고그룹의 화면감춤을 설정한다 
        바코드입력란으로 초점을 이동한다.      
}
창고닫기를 누르면 다음 문단을 실행한다
{
        창고그룹의 화면감춤을 설정한다
        바코드입력란으로 초점을 이동한다
}
// 불출위치/////////////////////////////////////////
불출입력란을 누르면 다음 문단을 실행한다
{
        불출그룹의 화면감춤을 해제한다
        DB는 데이터베이스("SHTECH_DB_PDA")를 참조한다
        질의문은 식(" select NAME as STOR_NAME, CODE as STOR_CODE  from SYS_CODE_INFO where GRUP_CODE = 'KITT_GUBN'")을 참조한다
        명세서배열값은 ""이다
        DB의 질의실행(질의문, 명세서배열값)을 실행한다
        불출검색창은 명세서배열값을 참조한다
}
불출검색창을 누르면 다음 문단을 실행한다
{
        선택행은 창고검색창의 선택행을 참조한다
        불출입력란은 불출검색창(선택행, "STOR_NAME")을 참조한다
        불출창고코드는 불출검색창(선택행, "STOR_CODE")을 참조한다
        불출그룹의 화면감춤을 설정한다
        바코드입력란으로 초점을 이동한다.
}
불출닫기를 누르면 다음 문단을 실행한다
{
        불출그룹의 화면감춤을 설정한다
        바코드입력란으로 초점을 이동한다
}
////////////////////////////////////////////

// 작업자변경////////////////////////////////////////////////
사용자를 누르면 다음 문단을 실행한다
{
        작업자그룹의 화면감춤을 해제한다
        DB는 데이터베이스("SHTECH_DB_PDA")를 참조한다
        질의문은 식(" select USER_CODE AS CODE_USER, USER_NAME AS NAME_USER  
                          from BE_USER_INFO 
                          WHERE USER_CODE IN('SH0001', 'SH0002', 'SH0003', 'SH0004', 'SH0005')")을 참조한다
        작업자배열값은 ""이다
        DB의 질의실행(질의문, 작업자배열값)을 실행한다
        작업자변경은 작업자배열값을 참조한다
}
작업자변경을 누르면 다음 문단을 실행한다
{
        선택행은 작업자변경의 선택행을 참조한다
        사용자는 작업자변경(선택행, "NAME_USER")을 참조한다
        사용자코드는 작업자변경(선택행, "CODE_USER")을 참조한다

        사원명은 작업자변경(선택행, "NAME_USER")을 참조한다
        사원코드는 작업자변경(선택행, "CODE_USER")을 참조한다
        불출사원명은 작업자변경(선택행, "NAME_USER")을 참조한다
        불출사원코드는 작업자변경(선택행, "CODE_USER")을 참조한다
        전역정보의 "사원명"을 사원명로 설정한다
        전역정보의 "사원코드"를 사원코드로 설정한다
        전역정보의 "불출사원명"을 불출사원명로 설정한다
        전역정보의 "불출사원코드"를 불출사원코드로 설정한다
        작업자그룹의 화면감춤을 설정한다
        바코드입력란으로 초점을 이동한다

}
작업자변경닫기를 누르면 다음 문단을 실행한다
{
        작업자그룹의 화면감춤을 설정한다
        바코드입력란으로 초점을 이동한다
}
/////////////////////////////////////////////////////////////////////
저장을 누르면 다음 문단을 실행한다.
{
        DB처리문("불출저장하기")을 실행한다.
        DB처리문("임시불출삭제")을 실행한다.
        "불출처리가 완료되었습니다."를 화면에 표시한다.
        폼을 종료한다.
}
취소를 누르면 다음 문단을 실행한다.
{
        DB처리문("임시불출삭제")을 실행한다.
        임시불출불러오기를 실행한다
}









