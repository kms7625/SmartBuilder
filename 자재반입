폼을 열면 다음 문단을 실행한다.
{
        사원명은 전역정보의 "사원명"이다
        사원코드는 전역정보의 "사원코드"이다
        사용자는 전역정보의 "사원명"이다
        전역정보의 "지정폼"은 5이다
 //       거래처그룹의 화면감춤을 설정한다
        창고그룹1의 화면감춤을 설정한다
//        사용자는 전역정보의 "사원코드"이다
       
}
홈버튼을 누르면 다음 문단을 실행한다
{
        "화면을 종료하시겠습니까?"를 화면에 표시한다('예아니오','질문',"홈화면","홈화면예처리","홈화면아니오처리")
}
문단 홈화면예처리
{
        폼을 종료한다
}
문단 홈화면아니오처리
{
        문단의 실행을 취소한다
}
바코드입력란에서 처리항목을 이동하면 다음 문단을 실행한다(탭번호, 탭이동유형)
{
       DB처리문("임시반입저장")을 실행한다.
       임시반입불러오기를 실행한다.
       바코드입력란은 ""을 참조한다
       바코드입력란으로 초점을 이동한다.
       //바코드체크1은 조회결과를 참조한다.
       //바코드체크1은 조회결과의 값을 대입한다.
}
문단 임시반입불러오기
{
        //DB처리문("임시반입불러오기")을 실행한다.
        // 임시반입조회창은 조회결과를 참조한다.
// 임시반입불러오기 SQL직접넣음
        DB는 데이터베이스("SHTECH_DB_PDA")를 참조한다

        질의문1은 식(" SELECT A.IPGO_NUMB, B.ATSK_CODE, A.ITEM_QNTY ")를 참조한다
        질의문2는 식(" FROM ST_MOVE_INFO_TEMP AS A ")를 참조한다
        질의문3은 식(" LEFT JOIN BE_ITEM_INFO AS B ON A.ITEM_CODE = B.ITEM_CODE ")를 참조한다
        질의문4는 식(" WHERE A.IN_USER = '"+사원코드+"' ")를 참조한다
        질의문5는 식("   AND A.MEMO_XXXX = 'PDA등록' ")을 참조한다
        질의문6은 식("   AND A.MOVE_DATE = '"+반입일자+"' ")를 참조한다
        질의문7는 식("   AND A.MOVE_TYPE = '22' ")를 참조한다

        질의문은 식(질의문1 + 질의문2 + 질의문3 + 질의문4 + 질의문5 + 질의문6 + 질의문7)을 참조한다

        결과배열값은 ""이다
        DB의 질의실행(질의문, 결과배열값)을 실행한다
        임시반입조회창은 결과배열값을 참조한다

 }
창고입력란을 누르면 다음 문단을 실행한다
{
        창고그룹1의 화면감춤을 해제한다
        DB는 데이터베이스("SHTECH_DB_PDA")를 참조한다
        질의문은 식(" SELECT STOR_NAME, STOR_CODE FROM BE_STOR_INFO ")을 참조한다
        명세서배열값은 ""이다
        DB의 질의실행(질의문, 명세서배열값)을 실행한다
        창고검색창1은 명세서배열값을 참조한다
}
창고검색창1을 누르면 다음 문단을 실행한다
{
        선택행은 창고검색창1의 선택행을 참조한다
        창고입력란은 창고검색창1(선택행, "STOR_NAME")을 참조한다
        창고코드는 창고검색창1(선택행, "STOR_CODE")을 참조한다
        창고그룹1의 화면감춤을 설정한다
}
창고닫기1를 누르면 다음 문단을 실행한다
{
        창고그룹1의 화면감춤을 설정한다
}
저장을 누르면 다음 문단을 실행한다.
{
        DB처리문("반입저장하기")을 실행한다.
        DB처리문("임시반입삭제")을 실행한다.
        "반입처리가 완료되었습니다."를 화면에 표시한다.
        폼을 종료한다.
}
취소를 누르면 다음 문단을 실행한다.
{
        DB처리문("임시반입삭제")을 실행한다.
        임시반입불러오기를 실행한다.
}






